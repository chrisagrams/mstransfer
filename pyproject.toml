[project]
name = "mstransfer"
version = "0.1.1"
description = "Transfer mass spectrometry files (mzML/MSZ) between endpoints using mscompress"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mscompress",
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "rich",
    "python-multipart",
    "pydantic>=2.0",
    "aiofiles",
]

[project.scripts]
mstransfer = "mstransfer.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mstransfer"]

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "SIM", "TCH", "PLC0415"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = ["mscompress.*", "aiofiles.*"]
ignore_missing_imports = true

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "httpx",
    "ruff",
    "pre-commit",
    "pyright>=1.1.408",
    "mypy>=1.10",
]
