[project]
name = "mstransfer-benchmark"
version = "0.1.0"
description = "Benchmark plotting for mstransfer"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
]

[tool.ruff]
line-length = 88
